---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Ricardo - Web Application Developer">
	<div class="hero">
		<div class="hero-content">
			<div class="title-wrapper">
				<h1>Ricardo Sánchez</h1>
				<div class="subtitle">Senior Web Application Developer en PSI Mobile</div>
				<div class="subtitle">Stripe Community Expert for Angular</div>
			</div>
			<p class="description">
				Since childhood, I have been interested in technology and how simple parts
				come together to perform complex tasks.
				Currently, I develop IT projects as a freelancer and lead mobile application development at PSI Mobile.
			</p>
			<div class="cta-buttons">
				<a href="/projects" class="button primary">View Projects</a>
				<a href="https://github.com/richnologies" target="_blank" class="button github">GitHub</a>
				<a href="https://www.linkedin.com/in/ricsanchez87" target="_blank" class="button linkedin">LinkedIn</a>
			</div>
		</div>
		<div class="hero-background">
			<div class="gradient-circle"></div>
			<div class="grid"></div>
		</div>
	</div>
</Layout>

<style>
	.hero {
		min-height: calc(100vh - 7rem);
		display: flex;
		align-items: center;
		position: relative;
		overflow: hidden;
	}
	.hero-content {
		max-width: 800px;
		position: relative;
		z-index: 2;
	}
	.title-wrapper {
		margin-bottom: 1.5rem;
	}
	h1 {
		font-size: 5rem;
		font-weight: 700;
		color: var(--text-primary);
		line-height: 1;
		margin-bottom: 1rem;
		background: linear-gradient(to right, var(--accent), var(--accent-light));
		-webkit-background-clip: text;
		background-clip: text;
		-webkit-text-fill-color: transparent;
		filter: drop-shadow(0 0 2em rgba(255, 87, 34, 0.2));
	}
	.subtitle {
		font-size: 1.5rem;
		color: var(--text-secondary);
		font-weight: 500;
	}
	.description {
		font-size: 1.25rem;
		color: var(--text-secondary);
		margin-bottom: 2rem;
		line-height: 1.6;
		max-width: 600px;
	}
	.highlight {
		color: var(--accent);
		text-decoration: none;
		position: relative;
		font-weight: 500;
	}
	.highlight::after {
		content: '';
		position: absolute;
		width: 100%;
		height: 2px;
		bottom: -2px;
		left: 0;
		background: var(--accent);
		transform: scaleX(0);
		transform-origin: right;
		transition: transform 0.3s ease;
	}
	.highlight:hover::after {
		transform: scaleX(1);
		transform-origin: left;
	}
	.cta-buttons {
		display: flex;
		gap: 1rem;
	}
	.button {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.8rem 1.6rem;
		border-radius: 8px;
		text-decoration: none;
		font-weight: 500;
		transition: all 0.3s ease;
		border: 1px solid transparent;
	}
	.button.primary {
		background: var(--accent);
		color: white;
	}
	.button.primary:hover {
		background: var(--accent-dark);
		transform: translateY(-2px);
	}
	.button.github {
		background: var(--card-bg);
		color: var(--text-primary);
		border-color: rgba(255, 255, 255, 0.1);
	}
	.button.github:hover {
		background: #333;
		transform: translateY(-2px);
	}
	.button.linkedin {
		background: var(--linkedin-blue);
		color: var(--text-primary);
		border-color: var(--linkedin-blue-dark);
	}
	.button.linkedin:hover {
		background: var(--linkedin-blue-dark);
		transform: translateY(-2px);
	}
	.hero-background {
		position: absolute;
		top: 0;
		right: 0;
		bottom: 0;
		left: 0;
		z-index: 1;
		overflow: hidden;
	}
	.gradient-circle {
		position: absolute;
		top: -50%;
		right: -20%;
		width: 80%;
		height: 150%;
		background: radial-gradient(circle, rgba(255, 87, 34, 0.1) 0%, rgba(255, 87, 34, 0) 70%);
		filter: blur(40px);
		animation: pulse 8s ease-in-out infinite;
	}
	.grid {
		position: absolute;
		top: 0;
		right: 0;
		bottom: 0;
		left: 0;
		background-image: linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px),
						linear-gradient(90deg, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
		background-size: 50px 50px;
		mask-image: radial-gradient(circle at 50% 50%, black 0%, transparent 80%);
	}
	@keyframes pulse {
		0%, 100% { transform: scale(1); }
		50% { transform: scale(1.2); }
	}
	@media (max-width: 768px) {
		h1 {
			font-size: 3.5rem;
		}
		.subtitle {
			font-size: 1.25rem;
		}
		.description {
			font-size: 1rem;
		}
		.cta-buttons {
			flex-direction: column;
		}
		.button {
			width: 100%;
			justify-content: center;
		}
	}
</style>
